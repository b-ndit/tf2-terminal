# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local settings can contain permission entries with
# secrets from ad-hoc test commands (e.g. API keys) - never commit these,
# regardless of the cloning machine's own global git ignore rules.
**/.claude/settings.local.json

# Test artifacts
test-results
playwright-report
e2e/screenshots
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
